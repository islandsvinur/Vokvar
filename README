Flow visualization
==================

To install the required libraries in Debian, run the following command:

  # aptitude install fftw-dev freeglut3-dev

To build it:

  $ make

To run it:

  $ ./fluids

Have fun!
Christian
